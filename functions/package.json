{
  "name": "jurye-functions",
  "private": true,
  "type": "module",
  "engines": { "node": "22" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node tests/architecture.test.mjs && node tests/pin.test.mjs && node tests/ollama-provider.test.mjs",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^7.0.0",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2"
  }
}
